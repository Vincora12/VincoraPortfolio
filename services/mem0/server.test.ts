import test from 'node:test';import assert from 'node:assert/strict';import {validateConfig} from './server.js';const b={VINZMON_MEMORY_SERVICE_SECRET:'x',QDRANT_URL:'https://q',QDRANT_API_KEY:'q',QDRANT_COLLECTION_NAME:'m',MEM0_LLM_API_KEY:'l',MEM0_EMBEDDER_API_KEY:'e',MEM0_HISTORY_DB_PATH:'/data/mem0-history.db'};test('config requires providers and /data history',()=>{assert.equal(validateConfig(b).history,'/data/mem0-history.db');assert.throws(()=>validateConfig({...b,QDRANT_API_KEY:''}));assert.throws(()=>validateConfig({...b,MEM0_HISTORY_DB_PATH:'/tmp/x'}))});
